function addTogether(i, j)
	:) super adder
	x = i + j
	return x
	
function yay()
	print "Yay"

import agenda.ics
build schedule
	cancel Dinner
	if not Aquila in Studytime[with]
		add Aquila to Studytime
	end
	for i = 0, i < 3, i = i + 1
		j = i + 1
		sum = addTogether(i, j)
		print sum
	end
	yay()
export new_agenda.ics
