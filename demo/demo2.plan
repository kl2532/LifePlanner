function addTogether(i, j)
	:) super adder
	x = i + j
	return x
	
function yay(n)
	i = 0
	while i<n
		print "Yay!"
		i = i + 1
	end

import agenda.ics
build schedule
	cancel Dinner
	if not Aquila in Studytime[with]
		add Aquila to Studytime
	end
	for i = 0, i < 3, i = i + 1
		j = i + 1
		sum = addTogether(i, j)
		print sum
	end
	yay(3)
export new_agenda.ics
